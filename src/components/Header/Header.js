import React, { Component } from 'react';

class Header extends Component {
    render() {
        return (
            <header>
                    React Components
            </header>
        );
    }
}

export default Header;